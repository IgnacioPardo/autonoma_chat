import Link from "next/link";

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center ">
      <div className="flex items-center justify-center bg-gradient-to-b from-primary-blue to-primary-violet text-white p-4 rounded-[10px] border border-border-violet shadow-lg w-full max-w-md">
        Chat with Autonoma AI
      </div>
    </main>
  );
}
